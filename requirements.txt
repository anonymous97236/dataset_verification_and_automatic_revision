# Run from the repository root with Python 3.10 or newer:
# python -m pip install -r requirements.txt

# Web interface
Flask>=3.0.0

# Gemini and OpenAI-compatible Qwen API clients
google-genai>=1.0.0
openai>=1.0.0

# Screenshot processing and visualization
Pillow>=10.0.0

# Install local modules for imports and `python -m pipeline` / `python -m web.app`.
-e .
